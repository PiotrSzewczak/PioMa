1.

Utworzyc plik docker compose, ktory bedzie odpalal skrypt run.py w jednym kontenerze.
w drugim kontnerze będzie baza danych postgres, ktora ma byc uruchomiona za pomocą tego samego docker compose.

2.

Pobrac dbeaver i utworzyc jakas prosta tabelke i zrobic do niej pare isertów w samym dbeaver

3. 

utworzyc modul ktory polaczy sie z baza danych i wyprintuje jakies dane ktore w niej utworzysz w dbeaerze.
te dane maja byc printowane jako obiekt dataframe z biblioteki pandas.
